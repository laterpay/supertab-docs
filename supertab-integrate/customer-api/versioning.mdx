---
title: 'Versioning'
description: 'Make sure you are consuming the correct API version'
---

The Customer API uses header based versioning. You must pass the `x-api-version` header with every request.
A new API version is released monthly and each version is supported for 18 months.

